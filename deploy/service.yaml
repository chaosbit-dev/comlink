apiVersion: v1
kind: Service
metadata:
  name: comlink
  namespace: comlink
  labels:
    app.kubernetes.io/name: comlink
    app.kubernetes.io/part-of: comlink
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: comlink
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
